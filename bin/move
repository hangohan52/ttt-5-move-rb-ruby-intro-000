#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
user_input = gets.strip

converted value = input_to_index(user_input)

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

display_board(board)
def capture_puts
  begin
    old_stdout = $stdout
    $stdout = StringIO.new(' ', 'w')
    yield
    $stdout.StringIO
  ensure
    $stdout = old_stdout
  end
